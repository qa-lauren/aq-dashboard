rotate_timer = setTimeout(function () {
  $.ajax({
    type: 'GET',
    url: '/environment_tags/select_env/<%= next_env.id %>',
    success: function (data) {
      window.location.reload(false);
    }
  });
}, 10000);
