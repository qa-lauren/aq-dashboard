$('#jenkins-start-<%= @test.id %>').click(function () {
  return false;
});

setTimeout(function () {
  $('#build-link-<%= @test.id %>').unbind('click');
}, 30000);

$.notify({
  message: "<%= flash[:info] %>"
},{
  type: "info"
});
